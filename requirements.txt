Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
requests==2.31.0
click==8.5.0
gunicorn==22.0.0
python-dotenv==1.0.0
tabulate==0.9.0
# cryptography: required for Pure1 JWT token generation (RS256 RSA signing)
# and for encrypting stored API credentials / proxy passwords.
cryptography>=41.0.0
pytz==2024.1
# 2.9.9 has no prebuilt wheel for newer Python (e.g. 3.13) on Windows → pip builds from .tar.gz and needs pg_config.
psycopg2-binary==2.9.10
openpyxl==3.1.2
pytest>=8.0.0
