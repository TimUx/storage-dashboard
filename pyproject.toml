[tool.ruff]
line-length = 120
target-version = "py310"
exclude = [
    "app/static",
    "venv",
    ".venv",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",
]

[tool.ruff.lint.per-file-ignores]
"app/migrations.py" = ["E402"]
